import { SolarSystem } from "@/components/SolarSystem";

export default function Home() {
  return (
    // SolarSystem contains the 3D Canvas and the 2D UI overlays (GameUI, PlanetFactCard)
    <SolarSystem />
  );
}
